// import firebase from 'react-native-firebase';
// // import database from '@react-native-firebase/database';
// import firebaseConfig from '../constants/firebase';

// const {
//   apiKey, authDomain, databaseURL, storageBucket, messagingSenderId, clientId, appId,
// } = firebaseConfig;

// let firebaseInitialized = false;

// if (apiKey && authDomain && databaseURL && storageBucket && messagingSenderId && clientId && appId) {
//   firebase.initializeApp({
//     apiKey, authDomain, databaseURL, storageBucket, messagingSenderId, clientId, appId,
//   });

//   firebaseInitialized = true;
// }

// export const FirebaseRef = firebaseInitialized ? firebase.database().ref() : null;
// export const Firebase = firebaseInitialized ? firebase : null;
